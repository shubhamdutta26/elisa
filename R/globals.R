# Declare global variables to avoid "no visible binding for global variable" note
utils::globalVariables(c("od450", "mean_blank", "blanked_od", "primary_mab",
                         "mean_od", "mean_sd", ".data", "x", "y"))
